# Local/dev image. App watch + bind-mounts are in docker-compose.yml (app service).
FROM node:22-alpine

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm ci

COPY tsconfig.json ./
COPY src ./src

ARG PORT=8080
EXPOSE ${PORT}

ENV NODE_ENV=development

CMD ["npx", "tsx", "src/server.ts"]
